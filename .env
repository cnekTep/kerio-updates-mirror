# Static project config, not meant to be edited by regular users.
# Defines which compose files are combined for a plain "docker compose up/down"
# (full stack: mirror + nginx + tor + xray).
COMPOSE_FILE=compose.mirror.yml:compose.nginx.yml:compose.tor.yml:compose.xray.yml